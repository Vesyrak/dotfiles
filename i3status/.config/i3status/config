general {
    output_format = "i3bar"
        colors = true
        interval = 5
        color_bad = "#b54036"
        color_degraded="#deb566"
        color_good="#5ab977"
}

order += "weather_yahoo"
order += "mpd_status"
order += "volume_status"
order += "disk /home"
order += "battery 0"
order += "load"
order += "backlight"
order += "arch_updates"
order += "tztime local"


weather_yahoo{
    woeid="967852"
        format_today = "Now: {icon}{temp}°{units} {text}"

}

mpd_status{
    host = "10.0.1.30"
        color_play = "#5ab977"
        color_pause="#6a7c93"
        color_stop="#b54036"
        on_click 1 = "exec mpc -h 10.0.1.30 prev"
        on_click 2 = "exec mpc -h 10.0.1.30 toggle"
        on_click 3 = "exec mpc -h 10.0.1.30 next"
        on_click 4 = "exec mpc -h 10.0.1.30 volume -4"
        on_click 5 = "exec mpc -h 10.0.1.30 volume +4"
        cache_timeout = 1
        state_play = "Playing: "
        state_pause = "Paused: "
}

volume_status {
    button_up = 4
        button_down = 5
        button_mute = 2
        command="pamixer"
}


battery 0 {
    format = "%status %percentage %remaining %emptytime"
        format_down = "No battery"
        status_chr = "⚡ CHR"
        status_bat = "🔋 BAT"
        status_unk = "? UNK"
        status_full = "☻ FULL"
        path = "/sys/class/power_supply/BAT%d/uevent"
        low_threshold = 10
}

backlight {

}

arch_updates {
    hide_if_zero=true
        include_aur=true
}

tztime local {
    format = "%d-%m-%Y %H:%M:%S"
}

load {
    format = "%5min"
}

disk "/" {
    format = "%free"
}
